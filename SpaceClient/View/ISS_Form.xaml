<Page x:Class="SpaceClient.View.ISS_Form"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpaceClient.View"
        xmlns:Wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" 
        mc:Ignorable="d"
        Title="ISS Tracking Center" Height="650" Width="900"
        >

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.RowSpan="2" Source="/Pictures/background.png" Stretch="UniformToFill" Opacity="0.3" Margin="-30" RenderTransformOrigin="0.5,0.5" Panel.ZIndex="-1">
            <Image.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-0.794"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="ISS ORBITAL TRACKER" Foreground="#FFFD71CB" 
                       FontSize="28" FontFamily="Bauhaus 93" Margin="0,0,0,5">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#FFFD71CB" BlurRadius="10" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="REAL-TIME TELEMETRY SENSOR" Foreground="#FFAECFFF" 
                       FontSize="14" FontFamily="Consolas" Opacity="0.7"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#AA0D162D" CornerRadius="12" 
                    BorderBrush="#FF1E2947" BorderThickness="2" Margin="0,0,20,0" Padding="2" ClipToBounds="True">

                <Wpf:WebView2 HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

            </Border>

            <Border Grid.Column="1" Background="#AA0D162D" CornerRadius="12" 
                    BorderBrush="#FF1E2947" BorderThickness="2">

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"/>

            </Border>
        </Grid>
    </Grid>
</Page>