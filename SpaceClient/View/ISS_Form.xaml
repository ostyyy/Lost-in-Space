<Page x:Class="SpaceClient.View.ISS_Form"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:SpaceClient.View"
      xmlns:Wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" 
      mc:Ignorable="d"
      Title="ISS Tracking Center" Background="#FF060B19" Foreground="White"
      Unloaded="Page_Unloaded"
      d:DesignHeight="650" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.RowSpan="2" Source="/Pictures/background.png" Stretch="UniformToFill" Opacity="0.3" Margin="-20" Panel.ZIndex="-1">
            <Image.RenderTransform>
                <TransformGroup>
                    <RotateTransform Angle="0"/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="ISS ORBITAL TRACKER" Foreground="#FFFD71CB" 
                       FontSize="24" FontFamily="Bauhaus 93" Margin="0,0,0,5">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#FFFD71CB" BlurRadius="10" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="REAL-TIME TELEMETRY SENSOR" Foreground="#FFAECFFF" 
                       FontSize="13" FontFamily="Consolas" Opacity="0.7"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="121*"/>
                <ColumnDefinition Width="395*"/>
                <ColumnDefinition Width="344*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#AA0D162D" CornerRadius="10" 
                    BorderBrush="#FF24335E" BorderThickness="2" Margin="0,0,15,0" Padding="2" ClipToBounds="True" Grid.ColumnSpan="2">

                <Wpf:WebView2 x:Name="MyWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            </Border>

            <Border Grid.Column="2" Background="#CC0D162D" BorderBrush="#FF1E2947" BorderThickness="1" CornerRadius="10" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                        <StackPanel>
                            <Rectangle Height="1" Fill="#FF24335E" Margin="0,0,0,10"/>

                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>