<Page x:Class="SpaceClient.View.PostDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Post Discussion" d:DesignHeight="650" d:DesignWidth="900"
      Background="#FF060B19">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="⬅ BACK TO FORUM" HorizontalAlignment="Left" Padding="15,8" 
                FontSize="12" FontFamily="Bauhaus 93" Cursor="Hand" Click="Back_Click"
                Background="#FF1E0F45" Foreground="#FFAECFFF" BorderBrush="#FF3F1975">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,5"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Border Grid.Row="1" Background="#AA0D162D" BorderBrush="#FF1E2947" BorderThickness="2" CornerRadius="12" Margin="0,20,0,20" Padding="20">
            <StackPanel>
                <TextBlock x:Name="TxtPostTitle" Text="Post Title" Foreground="#FFAECFFF" FontSize="24" FontFamily="Bauhaus 93"/>
                <TextBlock x:Name="TxtPostTopic" Text="Topic:" Foreground="#FF64748B" FontSize="16" FontFamily="Consolas" Margin="0,5,0,15"/>
                <TextBlock x:Name="TxtPostContent" Text="Content..." Foreground="White" FontSize="16" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <TextBlock Text="DISCUSSION" Foreground="#FFFD71CB" FontFamily="Bauhaus 93" FontSize="18"/>
        </Grid>
    </Grid>
</Page>